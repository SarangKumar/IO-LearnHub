function bmi(w, h) {
  return w / (h * h);
}

// console.log(bmi(50, 1.8))

const bmi_Arr = (w, h) => {
  console.log((w / h) * h);
  return 'anything'
};
bmi_Arr(50, 1)

const greet = () => 'hello'
console.log(greet())